server:
  port: 8080 # comment4
apiVersion: v1
# comment3
kind: Service
metadata:
  name: sample-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      protocol: TCP
  selector:
    app: demo-app
