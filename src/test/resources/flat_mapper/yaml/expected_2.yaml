server:
  port: 8080
  name: Apache Tomcat
apiVersion: v1
kind: Service
metadata:
  name: sample-service
spec:
  type: ClusterIP
  ports:
    - port: 8080
      protocol: TCP
  selector:
    app: demo-app
version: 3.0.0
